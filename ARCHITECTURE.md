# 시스템 아키텍처

## 🏗️ 전체 아키텍처

```
┌─────────────────────────────────────────────────────────────────┐
│                         External Users                           │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
                    ┌─────────────────┐
                    │   LoadBalancer   │
                    │   (or NodePort)  │
                    └────────┬─────────┘
                             │
                             ▼
        ┌────────────────────────────────────────────────┐
        │           API Gateway (Port 8080)              │
        │        Spring Cloud Gateway                    │
        │  - Routing                                     │
        │  - Load Balancing                              │
        │  - Rate Limiting                               │
        └─────┬──────────┬───────────┬──────────┬────────┘
              │          │           │          │
              ▼          ▼           ▼          ▼
    ┌─────────────┐  ┌──────────────┐  ┌──────────────┐
    │User Service │  │   Payment    │  │ Settlement   │
    │  (8081)     │  │  Service     │  │  Service     │
    │             │  │  (8082) ⭐   │  │  (8083) ⭐   │
    └──────┬──────┘  └──────┬───────┘  └──────┬───────┘
           │                │                  │
           ▼                ▼                  ▼
    ┌─────────────┐  ┌──────────────┐  ┌──────────────┐
    │  Partner    │  │ Accounting   │  │              │
    │  Service    │  │  Service     │  │              │
    │  (8084)     │  │  (8085) ⭐   │  │              │
    └──────┬──────┘  └──────┬───────┘  └──────────────┘
           │                │
           └────────┬───────┘
                    │
        ┌───────────┴───────────────────────────────────┐
        │                                               │
        ▼                      ▼                        ▼
┌─────────────────┐   ┌──────────────┐     ┌────────────────────┐
│   PostgreSQL    │   │    Redis     │     │       Kafka        │
│                 │   │   Sentinel   │     │   + Zookeeper      │
│  - Dev: Single  │   │              │     │                    │
│  - Prod: HA     │   │ Master + 2   │     │  3 Brokers +       │
│    Cluster      │   │ Replicas +   │     │  3 Zookeepers      │
│                 │   │ 3 Sentinels  │     │                    │
└─────────────────┘   └──────────────┘     └────────────────────┘
```

## 🔄 데이터 흐름

### 결제 처리 플로우

```
User Request
    │
    ▼
API Gateway ──────────────────────────┐
    │                                 │
    ▼                                 │
Payment Service                       │
    │                                 │
    ├──► PostgreSQL (결제 내역 저장)   │
    │                                 │
    ├──► Redis (중복 방지 캐시)        │
    │                                 │
    └──► Kafka (결제 완료 이벤트)      │
         │                            │
         ▼                            │
    Settlement Service ◄──────────────┘
         │
         ├──► PostgreSQL (정산 데이터)
         │
         └──► Kafka (정산 완료 이벤트)
              │
              ▼
         Accounting Service
              │
              └──► PostgreSQL (회계 전표)
```

### 이벤트 기반 통신

```
┌───────────────────────────────────────────────────────────┐
│                        Kafka Topics                       │
├───────────────────────────────────────────────────────────┤
│                                                           │
│  payment-requested     ─────────► Payment Service        │
│  payment-completed     ─────────► Settlement Service     │
│  payment-failed        ─────────► Notification Service   │
│  settlement-requested  ─────────► Settlement Service     │
│  settlement-completed  ─────────► Accounting Service     │
│  accounting-entry      ─────────► Reporting Service      │
│                                                           │
└───────────────────────────────────────────────────────────┘
```

## 🗄️ 데이터 저장소 구조

### PostgreSQL 데이터베이스 분리

```
┌────────────────────────────────────────────┐
│         PostgreSQL Cluster/Instance        │
├────────────────────────────────────────────┤
│                                            │
│  📊 userdb                                 │
│  ├─ users (사용자 정보)                     │
│  ├─ roles (권한)                           │
│  └─ sessions (세션)                        │
│                                            │
│  💳 paymentdb                              │
│  ├─ payments (결제 내역)                    │
│  ├─ payment_methods (결제 수단)             │
│  └─ transactions (트랜잭션)                 │
│                                            │
│  💰 settlementdb                           │
│  ├─ settlements (정산 정보)                 │
│  ├─ settlement_details (정산 상세)          │
│  └─ settlement_schedules (정산 스케줄)      │
│                                            │
│  🤝 partnerdb                              │
│  ├─ partners (파트너사 정보)                │
│  ├─ contracts (계약)                       │
│  └─ commissions (수수료)                   │
│                                            │
│  📖 accountingdb                           │
│  ├─ journal_entries (회계 전표)            │
│  ├─ ledgers (원장)                         │
│  └─ accounts (계정)                        │
│                                            │
└────────────────────────────────────────────┘
```

### Redis 캐시 전략

```
┌────────────────────────────────────────────┐
│            Redis Sentinel                  │
│         (Master + 2 Replicas)              │
├────────────────────────────────────────────┤
│                                            │
│  🔐 Session Cache                          │
│  ├─ session:{user_id}                      │
│  └─ TTL: 30 minutes                        │
│                                            │
│  💳 Payment Idempotency                    │
│  ├─ payment:{idempotency_key}              │
│  └─ TTL: 24 hours                          │
│                                            │
│  👤 User Cache                             │
│  ├─ user:{user_id}                         │
│  └─ TTL: 5 minutes                         │
│                                            │
│  🎯 Rate Limiting                          │
│  ├─ rate_limit:{ip}:{endpoint}             │
│  └─ TTL: 1 minute                          │
│                                            │
└────────────────────────────────────────────┘
```

## 🌐 네트워크 구성

### Kubernetes Service Mesh

```
microservices Namespace
│
├── Services (ClusterIP)
│   ├── postgresql.microservices.svc.cluster.local:5432
│   ├── redis-master.microservices.svc.cluster.local:6379
│   ├── redis-sentinel.microservices.svc.cluster.local:26379
│   ├── kafka.microservices.svc.cluster.local:9092
│   ├── api-gateway.microservices.svc.cluster.local:8080
│   ├── user-service.microservices.svc.cluster.local:8081
│   ├── payment-service.microservices.svc.cluster.local:8082
│   ├── settlement-service.microservices.svc.cluster.local:8083
│   ├── partner-service.microservices.svc.cluster.local:8084
│   └── accounting-service.microservices.svc.cluster.local:8085
│
└── External Access
    └── api-gateway (LoadBalancer/NodePort)
```

## 🔒 보안 계층

```
┌─────────────────────────────────────────────────────────┐
│                    Security Layers                      │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  1. Network Policies                                    │
│     - Pod간 통신 제어                                    │
│     - Namespace 격리                                     │
│                                                         │
│  2. RBAC (Role-Based Access Control)                   │
│     - ServiceAccount 권한 관리                           │
│     - API 접근 제어                                      │
│                                                         │
│  3. Secrets Management                                 │
│     - DB 비밀번호                                        │
│     - API Keys                                          │
│     - TLS Certificates                                  │
│                                                         │
│  4. TLS/SSL Encryption                                 │
│     - Pod간 통신 암호화                                  │
│     - 외부 통신 암호화                                   │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

## 📈 확장성

### Horizontal Pod Autoscaling (HPA)

```
┌─────────────────────────────────────────┐
│          HPA Configuration              │
├─────────────────────────────────────────┤
│                                         │
│  API Gateway:       3-10 pods          │
│  Payment Service:   5-20 pods ⭐       │
│  Settlement Svc:    3-15 pods          │
│  Accounting Svc:    3-15 pods          │
│  User Service:      3-10 pods          │
│  Partner Service:   2-8 pods           │
│                                         │
│  Metrics:                               │
│  - CPU: 60-70%                          │
│  - Memory: 75-80%                       │
│                                         │
└─────────────────────────────────────────┘
```

## 🔄 고가용성 (HA)

### 운영 환경 구성

```
┌────────────────────────────────────────────────┐
│              High Availability                 │
├────────────────────────────────────────────────┤
│                                                │
│  PostgreSQL:                                   │
│  ├─ Master (Read/Write)                        │
│  └─ Replica x2 (Read + Auto-failover)          │
│                                                │
│  Redis:                                        │
│  ├─ Master x1                                  │
│  ├─ Replica x2                                 │
│  └─ Sentinel x3 (Monitoring + Failover)        │
│                                                │
│  Kafka:                                        │
│  ├─ Broker x3                                  │
│  └─ Zookeeper x3                               │
│                                                │
│  Application Pods:                             │
│  ├─ Multiple replicas per service              │
│  ├─ Anti-affinity rules                        │
│  └─ Pod Disruption Budget                      │
│                                                │
└────────────────────────────────────────────────┘
```

## 🎯 트래픽 관리

```
                     ┌──────────────┐
                     │ LoadBalancer │
                     └──────┬───────┘
                            │
                    ┌───────▼────────┐
                    │  API Gateway   │
                    │                │
                    │  Features:     │
                    │  - Routing     │
                    │  - Retry       │
                    │  - Timeout     │
                    │  - Circuit     │
                    │    Breaker     │
                    └───────┬────────┘
                            │
        ┌───────────┬───────┼───────┬────────────┐
        │           │       │       │            │
        ▼           ▼       ▼       ▼            ▼
    ┌──────┐   ┌──────┐ ┌──────┐ ┌──────┐  ┌──────┐
    │User  │   │Pay   │ │Settle│ │Partner│ │Acct  │
    │Svc   │   │Svc   │ │Svc   │ │Svc   │  │Svc   │
    │      │   │      │ │      │ │      │  │      │
    │Pod x3│   │Pod x5│ │Pod x3│ │Pod x2│  │Pod x3│
    └──────┘   └──────┘ └──────┘ └──────┘  └──────┘
```

## 📊 모니터링 & 로깅

```
┌─────────────────────────────────────────────┐
│         Observability Stack                 │
├─────────────────────────────────────────────┤
│                                             │
│  📈 Metrics (Prometheus)                    │
│  ├─ Application metrics                     │
│  ├─ System metrics                          │
│  └─ Custom business metrics                 │
│                                             │
│  📊 Visualization (Grafana)                 │
│  ├─ Service dashboards                      │
│  ├─ Infrastructure dashboards               │
│  └─ Business KPI dashboards                 │
│                                             │
│  📝 Logging (ELK Stack)                     │
│  ├─ Application logs                        │
│  ├─ System logs                             │
│  └─ Audit logs                              │
│                                             │
│  🔍 Tracing (Jaeger/Zipkin)                │
│  └─ Distributed tracing                     │
│                                             │
└─────────────────────────────────────────────┘
```

---

## 🚀 배포 전략

```
┌────────────────────────────────────────┐
│       Deployment Strategy              │
├────────────────────────────────────────┤
│                                        │
│  Rolling Update:                       │
│  ├─ maxSurge: 25%                      │
│  └─ maxUnavailable: 25%                │
│                                        │
│  Blue-Green (Optional):                │
│  ├─ New version deployment             │
│  ├─ Traffic switch                     │
│  └─ Old version cleanup                │
│                                        │
│  Canary (Optional):                    │
│  ├─ 10% traffic to new version         │
│  ├─ Monitor metrics                    │
│  └─ Gradual rollout                    │
│                                        │
└────────────────────────────────────────┘
```

이 아키텍처는 확장 가능하고, 고가용성을 제공하며, 마이크로서비스 패턴을 따릅니다.
