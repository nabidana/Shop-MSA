# 테스트 환경 - 인프라만 배포 (애플리케이션 서비스 제외)
# 이 파일은 Base의 특정 리소스만 참조

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Component

resources:
  # 네임스페이스
  - ../../base/namespace.yaml
  
  # PostgreSQL
  - ../../base/postgresql
  
  # Redis Sentinel
  - ../../base/redis
  
  # Kafka + Zookeeper
  - ../../base/kafka

# 애플리케이션 서비스는 제외
# - ../../base/api-gateway
# - ../../base/user-service
# - ../../base/payment-service
# - ../../base/settlement-service
# - ../../base/partner-service
# - ../../base/accounting-service
