# API Gateway Service 타입 변경 (개발환경은 NodePort 사용)
apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: shop-msa
spec:
  type: NodePort  # LoadBalancer 대신 NodePort 사용
  ports:
  - name: http
    port: 80
    targetPort: 8080
    nodePort: 30080  # 개발환경 고정 NodePort
    protocol: TCP
